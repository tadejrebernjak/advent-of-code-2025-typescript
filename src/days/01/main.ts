import type { Answer, Input, Solution } from "@/types/base.js";

const _parseInput = (input: Input) => {
  return input;
};

export const main: Solution = (_input: Input): Answer => {
  const part1 = null;
  const part2 = null;

  return {
    part1,
    part2,
  };
};
